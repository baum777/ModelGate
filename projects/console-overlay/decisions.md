# Decisions

## Durable Decisions

- Backend execution truth remains authoritative; the browser is a rendering and approval-intent shell.
- Daily work history belongs in `daily/`; stable product state belongs in `project.md`, `context.md`, and repo docs.
- Local logs and generated output are evidence aids, not canonical product truth.
